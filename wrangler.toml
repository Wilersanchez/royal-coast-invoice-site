name = "royal-coast-invoice-site"
main = ".open-next/worker/index.mjs"

# keep or bump; it's fine
compatibility_date = "2024-09-01"

# NEW: v4 syntax replaces `node_compat = true`
compatibility_flags = ["nodejs_compat"]